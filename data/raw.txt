拼写有误，peform => perform
`t('{documentType} Number', {documentType: props.documentTitle})`
 documentTitle 最好改成documentType
[摩根律](https://zh.wikipedia.org/wiki/%E5%BE%B7%E6%91%A9%E6%A0%B9%E5%AE%9A%E5%BE%8B)
 ```js
 (props.viewOnly && !props.partialEdit) ||
 !props.line.isTaxClassificationCodeValid ||
 !props.line.isTaxClassificationCodeLengthValid
 ```
(!props.viewOnly || props.partialEdit) &&
 (!props.line.isTaxClassificationCodeValid ||
 !props.line.isTaxClassificationCodeLengthValid)
 可编辑的情况下，并且不合法，才会显示error message;
这里加一个空格是为什么？
@USTC-Han 好像是个布局占位符？
这个是我加的，就是占位用的
![image](https://user-images.githubusercontent.com/38212028/50671291-e984f400-100b-11e9-97ec-4ea538df8b8e.png)
 ![image](https://user-images.githubusercontent.com/38212028/50671333-323cad00-100c-11e9-8963-d3e31ff518b6.png)
 
 这个部分有的状态是没有提示的，有的地方有提示，所以对没提示的用了占位符，这样样式统一点
有个疑问，这里翻译的键和值为啥要整成一样的，为啥不像上面那样简写？
这边全部都没用简写，就跟着这样了
groupStats，中间的s大写好一点
以后还会有新的figure吗？如果有的话，可以考虑用ng-repeat改造一下，减少重复代码
https://github.com/TradeshiftCN/Apps/tree/dev-stable/src/main/v4apps/apps/supplierManagement/SupplierOnboarding/src/client/directives/groupstats 从这里copy的。这部分的逻辑基本跟SupplierOnboarding是一样的
Fish说SupplierOnboarding是给admin用的，这个tab是给reseller用的。功能基本是一样的。
这个css基本上都是标签名，后期可能不利于他人维护，可以试试BEM方式
这里加了uploaded,就把failed改成fail吧，当名词。items failed感觉这个名词加形容词也可以
和sarah商量了下 换成了 upload successful 和 upload failed.
使用moment#isValid()方法判断是否为合法字符
 https://momentjs.com/docs/#/parsing/is-valid/
dev-stable上也要改下，两边都需要
这个文件也要删了吧？
改了现有的文字，对应的insightTranslationsHelper.service.js里的也改一下
ok
翻译可以直接加在app里了吧
@KanonWang 计划damo-7开始正式实行
@KanonWang 之前的pr，Chris要求翻译都放在CNTransitions里的
新的翻译流程会在Damo7开始实行。不过由于global已经迁移到新的翻译流程。我们现在使用老的翻译流程的时候，不需要考虑和global翻译冲突的问题了。可以把翻译先加在自己的app里。
 @KanonWang @muwenzi @08070113
@Chris-Xie 好的
if-else里都有的代码，可以移出来，写一遍就可以了。
response.result === true ? this._setAllTabs() : this._setNetworkTabs();
 this._setClickEvent(ts.ui.TabBar.tabs().length - 2, '/sellerManagement');
 这两行顺序不能换，提出来，就得再写一个if
这个文件跳过prettifier了吗？怎么都没有分号
button里面的文字不用大写，ts-ui会自动通过css把他们转成大写
![image](https://user-images.githubusercontent.com/12554487/50499105-c1344d00-0a81-11e9-9058-acbce41f09fb.png)
ui库是这样的 但是我觉得这是一个很不好的设计，会让其他开发者很困惑，也阻止了某些不需要大写的场景，而且它的设计是button里面必须包裹span才是大写 直接text还是正常的大小写
按道理，git commit有hook的，会触发lint的。
 ```
 ➜ Apps git:(dev-stable) ✗ gcam 'ab'
 Found '/Users/Chris/ts/Apps/.nvmrc' with version <8.11.1>
 Now using node v8.11.1 (npm v5.6.0)
 Found '/Users/Chris/ts/Apps/.nvmrc' with version <8.11.1>
 Now using node v8.11.1 (npm v5.6.0)
 husky > npm run -s precommit (node v8.11.1)
 
 ↓ Running tasks for README.md [skipped]
 → No staged files match README.md
 ✔ Running tasks for **/*.+(js|json|less|css|ts)
 [dev-stable 00f0625606] ab
 ```
你执行以下npm install再试试
把company直接作为一个props从外面传进来呢？
!!props.showIssueDate
可以为负数吗？我记得有个utils有个判断条件
刚刚看了下，目前支持填写负数的，后端也支持
在外面拿不到type的值，无法判断当前要显示哪个company
这两个都对应 Unit Price?
@USTC-Han Price一个P是大写，一个p是小写，用到的地方不一样，所以写了两个
哦，好吧，一眼没看出来
@muwenzi 这个key和上面的key重复了
这个不和上面的surveys冲突了吗
加 t 支持翻译
networkTabs和allTabs 不会同时使用
好哒，之后会都加好的
id名字用得更准确一点
这个暂时不用改，后端已经支持-1了
http://ui.tradeshift.com/v11/#components/buttons/buttons.html
 不需要自己去改写button
不需要，默认就是大写
![image](https://user-images.githubusercontent.com/12554487/50423821-87065800-0895-11e9-9d03-0db3acc2b42c.png)
 ![image](https://user-images.githubusercontent.com/12554487/50423826-8d94cf80-0895-11e9-9393-e099cb45f409.png)
 
 icon的button不能满足吗？
我改一下
dialog-sub-title1感觉这个命名有点奇怪，还有title2 ?
刚查了这个属性，直支持firefox和chrome,其他浏览器不支持，要不要加上 width: -moz-fit-content;
 width: -webkit-fit-content; @muwenzi
IE不兼容https://caniuse.com/#search=fit-content 换用flex
不需要用button了
换一个icon
建议把涉及到10px的替换成@ts-unit-half
这里的important是不是因为，不加important没法生效所以加上的？
@USTC-Han 这是需要覆盖TSUI默认样式，所以加了important
!(props.viewOnly || !props.quantityAndUnitCanEdit) && !props.line.isUnitValid这里面的quantityAndUnitCanEdit判断是不是可以去掉了
!props.quantityAndUnitCanEdit
 没看明白这个之前加的意义，||一个false和不写这个感觉没区别
```js
 _.isEmpty(vm.selectedConnections) ||
 _.isNull(vm.selectedPlanIndex) ||
 _.isNull(vm.form.startDate) ||
 _.isNull(vm.form.expireDate)
 ```
else 都去掉
默认返回 true还是false 最好写完整
用变量
这段逻辑取反有点绕，可读性有点差，有点难以维护
 
 ```js
 vm.applyPlanButton = _.isEmpty(vm.selectedConnections) ||
 _.isNull(vm.selectedPlanIndex) ||
 _.isNull(vm.form.startDate) ||
 _.isNull(vm.form.expireDate)
 ```
测一下电子邮件长字符的时候样式
![image](https://user-images.githubusercontent.com/38212028/50392622-e9266680-078a-11e9-842e-dc42dea5fd05.png)
![image](https://user-images.githubusercontent.com/38212028/50392885-1411ba00-078d-11e9-9a84-b93829cacc79.png)
 和设计商量一致，当邮件地址过长，设置折行
少一个`.`
设计稿没句号，我就没写
ruleName 用变量
ruleName 用变量
BEM名称 嵌套最好采用"继承父名"的方式
 比如最顶层
 ```less
 .fapiao-ap__panel {
 &-tip-info {} // .fapiao-ap__panel-tip-info
 &-main-content { // .fapiao-ap__panel-main-content
 &-title {} // .fapiao-ap__panel-main-content-title
 }
 }
 ```
ruleType 是干什么的？
get setting的时候用：ruleType找到相应的content，比如boolContent或strContent或groovyContent或listContent
 ![image](https://user-images.githubusercontent.com/10508637/50328985-3ce94380-0530-11e9-86df-77f7557ff88f.png)
 save setting的时候要把content的类型发回给后端：
 ![image](https://user-images.githubusercontent.com/10508637/50329091-9e111700-0530-11e9-9622-ead51cbb6fc1.png)
 
因为对于嵌套和继承的规则不是很清楚，又去请教了一下Leon，交流下来是推荐在modifier的时候用嵌套，其他元素都打平，因为bem本身可以准确定位元素和防止冲突，不推荐嵌套，避免b__e-e-e的情况，所以最后将元素进行了打平，同时使用继承，减少了多个元素前缀都是.fapiao-ap的冗余写法
这个api设计的返回字段，有点问题，太“弱智”冗余了，后面可以和后端讨论一下，这样定义的方式的合理性。目前先保持这样
大写 下划线
change name to isSettingValid
加!!props.issueDate
这个名字改下吧，这个时间不光是红票的开票时间。
需要再加个参数
 opt.params = {
 from: 'DocumentList'
 };
这里也需要加
 opt.params = {
 from: 'DocumentList'
 };
`timePeriodFilterId = 'DAY_TIME_PERIOD'`
可以删掉了
这个是fix global报表的两个bug的临时解决方案
 1，初始过滤条件没有timeperiod，选择了timerange之后就会有timeperiod的过滤条件
 2， 如果没有选择timerange，timeperiod就不会被赋值，点击update之后该方法就会跳过，页面无反应
这里有个debugger
这个引号不用改
没有手动改过这个地方 不知道是哪里出了问题
这个没有意义的。
这个不要换位置
js lint 语法检查通不过，要求我import置顶
名字改一下
这个大小写跟前面的不一致
`@ts-radius`
`@ts-fontweight-bold`
如果两层元素的css class的命名分别是 data-auto-fill 和 data-auto-fill-access的话，并且父级元素没有样式的话，感觉没有必要通过嵌套class要限定样式。
好的👌🏻，已经删除了不必要的代码
premade 也用的是loadProforma.action.js？
@muwenzi 是的，之前action没有做区分，只是后面调用的service的接口不一样
OK 那这个文件名后面sherry会在另一个pr上面改
能否加个webpack alias，然后用绝对路径？
好主意，不过这个是老外的app，要改尽量改在他们那边吧。
translation
中文问号`？`应该改成英文问号`?`
per
我们这边Tax是税率的意思，你们那是税额？英文要统一吗
上边那个Tax Rate是税率我们这
change to getErrorMessage(responseBody.code, runtime.bwts.language) || responseBody.message || xhr.statusText || err
`_.includes([...], this.documentStatus)`
用`[].includes()`？
IE11不支持，不确定global会不会做polyfill
已修正,可查阅
这个语法我们一直在用
 ```json
 "presets": [
 ["@babel/preset-env", { "targets": ["last 2 versions", "ie 11"] }],
 "@babel/preset-react"
 ]
 ```
 babel的配置应该是可以兼容ie11的
👍
直接 return _.every(filters, value => value.length === 0)
 hasFilterBar 已经具有一定含义了，即使用中间变量 bool也无意义，`_.every`返回值肯定是bool
判断有问题，只写了一个=
判断有问题，只写了一个=
缩进少空格
这边是否要用`env.TRANSLATIONS_AWARE_BRANCH`
同时将`package.json`里面的`script`去掉吧
这一整个函数可以重构一下
 switch array都可以
修改过了，可以看一下@muwenzi
这个迁移带出这个问题？看一下迁移前的记录
![image](https://user-images.githubusercontent.com/33616744/50429867-9d46ff00-08fb-11e9-9128-83a61daff01c.png)
 cn-pay-apps和v4apps的不一样
我知道，我等下看下cn-pay-apps rest那边库的问题，看看能不能两边保持一致
TOP_BAR_BUTTON_IDS不是已经改成object了吗
命名统一一下吧
premade-invoice-card__left--title 
 这个不符合Block__Element-Modifier的命名规范吧！
head
&-title title不是算是modify
同理
同理
同理
用npm的query-string来解析url参数：https://www.npmjs.com/package/query-string
- 说明一下这两个key的目的，干什么用的
 - 禁止个人修改key，语气强烈一点
那就再继续加上isString的判断
可以用箭头函数
form.append(... formFieldArguments)
实际文字应该里面用双引号
change name to clearSelection
使用标准单位
change class name to operate-area
change name to clear-selection-btn
multi lines
加翻译
翻译
_.get(this, 'invoice. itemRemarkList[0]. transferReasonCandidates', defaultValue)
`@ts-fontweight`
建议用最新的色值
`@ts-unit-quarter`
_.get($scope, 'item.pdfInvoiceView.lineItems', []).filter(item => item._editable)
这里还是用固定宽度
it should be better to change 300 to 500
remove ng-attr
icon 找cris 要一张 svg png会像素模糊
@Chris-Xie Apps那边其他app也在依赖CNTranslations
 新机制能否解决这个问题？
 还是放在cn的npm包里面？
好
没理解你的问题。新的翻译机制会使得我们和global的翻译仓库分离，所以CNTranslations可以不需要。
CNTranslation这个app不需要copy过来。
用@tradeshift/services package里的currency
是我理解错了，这个app应该不需要CNTranslations @AndrewWujiChen
notOnlyK8sChanged 是指？
指的是当前的commit修改的文件不止是kubernetes文件夹里面的改动。
 在只改了kubernetes文件夹里面的内容时，不需要其他的stage。
之前的属性换成多行
为什么要undefined？
autofocus和disable一样，是那种存在就有效的属性，会忽略属性的值，
 而ng-attr只有在属性值为undefined才移除元素，其他情况下都会加上元素
 详见https://docs.angularjs.org/guide/interpolation
`$index === 0 || undefined`
👍
